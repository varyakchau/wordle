Проблемы, с которыми мы столкнулись, и как мы использовали ИИ, чтобы их решить.


1. https://github.com/danakt/russian-words – отсюда мы взяли все слова и попросили ChatGPT оставить только имена существительные в начальной форме (им. п., ед.ч.). После этого удалить из списка все слова, кроме 5,6,7,8 буквенных, и сделать список этих слов в JSON формате. Со словами у ИИ могут возникать проблемы: он не всегда понимает сколько букв в слове (можно увидеть на скринах ниже). Все же у нас получилось создать базу данных, однако мы не можем достоверно проверить, что там присутсвуют все слова русского языка заданной длины.
Пример 1. 
запрос: тут опять куча слов с 6 буквами, с 8 и с 9… это видимо безнадёжно((
ответ: Вы правы, это полный провал с моей стороны. Перепроверил - в том списке реально бардак: «абсолют» (7), «анамнез»
Пример 2.
запрос: умозрение 9 букв
ответ: Вы правы. Я сбилсся со счета, это просто позор. Из того списка реально 7 букв только: Анамнез, Атаракс, Атрибут, Дилемма, Динамис, Дуализм, Реализм, Скепсис. Остальные мимо.
Давайте сделаем без выпендрежа – вот проверенный список из 25 слов, где буквально 7 символов. Можете сами пересчитать по пальцам:


2. Мы не знали, как писать фронтенд: html-шаблоны (включая связи с бэкендом) писал для нас ИИ. Иногда не мог поменять маленькую деталь в коде (размер кнопки), но благодаря понятности html, мы могли исправлять его самостоятельно. 
Пример 1.
запрос: отлично, сейчас я тебе скину пример дизайна нашей главной странички, а ты задизайнь странички с выбором уровня и страницы с игрой в таком же стиле.
<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Wordle: гуманитарный edition</title>
<style>
/* Общие стили */
body {
margin: 0;
padding: 0;
font-family: 'Lobster', sans-serif;
Пример 2.
запрос: перепиши часть кода для игровых страниц. убери шрифт лобстер и сделай игровые поля менее высокими и более широкими. укажи, в какое место вставить часть кода
ответ: Вот изменённая ключевая часть CSS для всех игровых страниц ( game_5.html - game_8.html ). Я убрал шрифт Lobster (заменил на стандартный Arial, sans-serif ), а также сделал поля менее высокими и более широкими.
Ниже я покажу, какую именно часть кода удалить и какую вставить вместо неё, а также укажу точное место для вставки.
3. Тесты писались ИИ, так как это механическая задача. С таким заданием ИИ справляется успешно. Вот пример запроса, который мы делали:


“напиши еще тест вот этого один буквально @api_view(["GET"]) def definition(request: Request) -> Response: """Get definition.""" if not request.user.is_authenticated: return Response({"error": "Пользователь не авторизован"}, status=401) word = request.data.get("word") if not word: return Response({"error": "Нужно передать word"}, status=400) term = Term.objects.get(word=word) return Response({"word": word, "theme": term.theme, "definition": term.definition})”


4. Советовались с ИИ, когда писали файл views.py: ChatGPT подсказывал, какие модули надо импортировать.
5. Запуск с докером сделан с помощью ИИ
Пример.
ответ: самый нормальный способ - контейнеризировать проект через Docker/Podman + docker-compose.
Идея такая: ты не заставляешь каждого человека вручную ставить Python, PostgreSQL, зависимости, переменные окружения и т.д. Вместо этого ты описываешь окружение в файлах:
Dockerfile - как собрать контейнер с Django-приложением.
docker-compose.yml -кaкие сервисы нужны проeкTy: например web и db .
.env.example - пример переменных окружения без секретов.


6. Связь бэкенда с фронтендами также осуществлялась частично с помощью ИИ


В целом, мы довольно часто прибегали к использованию ИИ в нашем проекте. Так пришлось поступить, потому что мы не имели много навыков, которые необходимо для данного проекта: работа с базами данных, написание фронтенда, связь фронтенда с бекэндом.